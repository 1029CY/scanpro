# 轻量级扫描助手小程序 PRD

| 文档版本 | 修改日期   | 修改人     | 修改内容     |
| :------- | :--------- | :--------- | :----------- |
| V1.0     | 2026-05-13 | [你的名字] | 初始版本创建 |

---

## 1. 产品概述

### 1.1 产品背景
个人日常有较多纸质文件电子化需求（如笔记、合同、书页），而市面现有App广告多、体积大、高级功能需付费。希望开发一款**无广告、极简、核心功能够用**的扫描小程序，供个人使用。

### 1.2 产品定位
一款专注于“拍—调—存”的轻量级文档扫描小程序，提供清晰、快捷的纸质文件电子化体验。

### 1.3 目标用户
- **主要用户**：开发者本人
- **潜在用户**：家人或极小范围分享使用的朋友（仅需基础扫描功能）

---

## 2. 核心功能与需求列表

| 功能模块       | 优先级   | 功能点     | 需求描述                                     |
| :------------- | :------- | :--------- | :------------------------------------------- |
| **相机拍照**   | P0(必须) | 拍照扫描   | 调用手机摄像头，拍摄文档                     |
|                | P0(必须) | 相册导入   | 支持从手机相册选择已有图片                   |
| **图片预处理** | P0(必须) | 自动切边   | 拍摄后，自动识别文档边缘并进行裁剪矫正       |
|                | P0(必须) | 手动调整   | 允许手动拖动四个角点，微调边缘               |
|                | P1(重要) | 自动增强   | 提供“原图、增强并锐化、黑白、灰度”等滤镜模式 |
| **结果与导出** | P0(必须) | 生成扫描件 | 将处理后的图片生成清晰的扫描件预览           |
|                | P0(必须) | 保存至相册 | 将单张或拼接后的图片保存到手机系统相册       |
|                | P0(必须) | 生成PDF    | 支持将1页或多页扫描件导出为一个PDF文件       |
| **基础管理**   | P1(重要) | 扫描历史   | 按时间倒序，列表展示历史扫描记录             |
|                | P1(重要) | 多页连扫   | 在一次任务中，连续拍摄多页，最后统一处理     |
|                | P2(可选) | 页面重排   | 在多页模式下，可拖拽调整页面顺序             |

**本期不做的功能（明确范围边界）：**
- OCR 文字识别（依赖昂贵的云服务或大型本地模型，个人版暂不涉及）
- 云存储/多端同步
- 文档分类文件夹
- 任何形式的社区、分享、广告

---

## 3. 核心流程

### 3.1 主要流程：单页扫描并保存
1. **启动** → 打开小程序，默认进入首页（扫描历史列表）。
2. **拍摄入口** → 点击底部悬浮“扫描”按钮，调起系统相机。
3. **拍照** → 对准文档拍摄。
4. **自动处理** → 系统自动进行边缘检测、裁剪和图像增强。
5. **确认/编辑** → 进入预览编辑页，用户可：
   - 手动微调裁剪区域。
   - 切换色彩增强模式（原色/增强/黑白/灰度）。
   - 若效果满意，点击“完成”；若需重拍，点击“返回重拍”。
6. **导出** → 点击“保存”，呼出选项：
   - “保存图片到相册”
   - “导出为PDF”
   - “继续扫描”（进入多页模式）
7. **结束** → 操作完成后，回到首页历史列表，最新记录出现在顶部。

---

## 4. 页面结构与功能详述

小程序共需约3个主页面。

### 页面一：首页（扫描历史）

- **顶部**：标题“我的扫描件”。
- **主体**：历史扫描记录列表，每条包含：
  - 扫描件缩略图（展示第一页）
  - 标题（默认显示为“扫描件_日期时间”）
  - 页数角标（如“3页”）
  - 可左滑删除某条记录
- **底部**：居中的大号“**+**”按钮，文字“开始扫描”，点击后进入拍照流程。
- **空状态**：当无任何记录时，页面中央显示一个扫描仪图标和文字“点击下方按钮，扫描第一份文件”。

### 页面二：相机/拍摄页

- 直接调用微信小程序原生相机组件。
- **辅助框**：半透明遮罩，中央为矩形取景框，提示用户将文档放入框内。
- **底部操作栏**：
  - 中间圆形快门按钮。
  - 左侧“相册”图标，点击可从相册导入。
  - 右侧“关闭”图标，点击返回首页。

### 页面三：编辑与预览页

- **预览区**：大面积展示处理后图片，可双指缩放查看细节。
- **多页缩略图栏**（连扫模式下出现在底部）：横向滑动查看已拍页面，可点击切换、拖拽排序或删除。
- **功能按钮栏**（位于预览下方）：
  - **裁剪**：点击后图片四角出现可拖拽的圆点，手动调整文档边缘。
  - **增强**：轮播切换滤镜（原色、增强并锐化、黑白、灰度），点击实时预览效果。
  - **重拍/删除**：删除当前页或重新拍摄。
- **底部固定栏**：
  - “添加页”按钮（仅在连扫模式显示）
  - “完成/保存”按钮 → 点击后弹出导出选项菜单。

---

## 5. 非功能需求

- **性能**：拍照后自动处理（裁剪+增强）的响应时间应在2秒以内，需要选择轻量高效的算法。
- **存储**：所有扫描文件及历史记录仅存储在用户手机本地（小程序缓存），不上传任何服务器。
- **隐私**：整个流程无网络请求，无用户数据收集，完全本地化运行，确保文件隐私安全。
- **兼容性**：需兼容 iOS 12+ 及 Android 7.0+ 主流机型，适配不同屏幕比例。

---

## 6. 数据与接口

### 6.1 本地数据结构
所有数据存入小程序本地缓存。

```json
{
  "scanRecords": [
    {
      "id": "唯一id",
      "createTime": "2026-05-13T10:30:00",
      "pageCount": 3,
      "pages": [
        {
          "pageId": "p1",
          "localImagePath": "本地临时路径/缓存路径",
          "filterType": "enhanced"
        }
      ]
    }
  ]
}
```

### 6.2 外部接口/插件

- 无服务端接口需求。
- 可考虑使用微信官方或社区成熟的**图像处理插件/JS库**，实现前端剪裁、滤镜功能，无需单独开发成像算法。